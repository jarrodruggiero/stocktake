apiVersion: v1
kind: Service
metadata:
  name: {{ include "stocktake.fullname" . }}
  labels: {{- include "stocktake.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector: {{- include "stocktake.selectorLabels" . | nindent 4 }}
